<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AboutThisPC.OverviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AboutThisPC"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="6" />
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="6*" />
            </Grid.ColumnDefinitions>

            <Image x:Name="WindowsIcon" Grid.Column="0" Width="135" Stretch="Uniform" HorizontalAlignment="Center" />
            <StackPanel Grid.Column="1" />

            <Grid Grid.Column="2" HorizontalAlignment="Left">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="10" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock x:Name="OSVersion" Grid.Row="0" FontSize="42" FontWeight="Normal" />
                <TextBlock x:Name="OSVersionSub" Grid.Row="1" FontSize="14" FontWeight="Normal" />
                <StackPanel Grid.Row="2" />
                <TextBlock x:Name="ComputerModelSub" Grid.Row="3" FontSize="11" />
                <StackPanel x:Name="OverviewStackPanelColumn" Grid.Row="4" Orientation="Vertical" />
            </Grid>
        </Grid>

        <StackPanel Grid.Row="1" />
        <StackPanel Grid.Row="2" x:Name="OverviewStackPanelBottomFooter" Orientation="Horizontal" HorizontalAlignment="Center">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" x:Name="OverviewStackPanelBottomText" Orientation="Vertical" VerticalAlignment="Center" />
                <StackPanel Grid.Column="1" x:Name="OverviewStackPanelColumnSpacer6" Orientation="Vertical" />
                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="10">
                    <Button Click="EyeButtonClick" x:Name="EyeButton">
                        <Grid Width="16" Height="16">
                            <FontIcon x:Name="EyeIconGlyph" FontFamily="Segoe MDL2 Assets" Glyph="&#xE7B3;" FontSize="16" />
                            <Line X1="0" Y2="0" Y1="16" X2="16" Stroke="{ThemeResource SystemControlForegroundBaseHighBrush}" StrokeThickness="2" x:Name="EyeIconLine" />
                        </Grid>
                    </Button>
                </StackPanel>
            </Grid>
        </StackPanel>
        <StackPanel Grid.Row="3" />
    </Grid>
</Page>
