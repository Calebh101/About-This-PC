<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AboutThisPC.DisplaysPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AboutThisPC"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Stretch">
            <ItemsRepeater ItemsSource="{x:Bind Displays}" HorizontalAlignment="Stretch">
                <ItemsRepeater.Layout>
                    <UniformGridLayout x:Name="MainLayout" Orientation="Horizontal" MinItemHeight="300" />
                </ItemsRepeater.Layout>

                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="local:DisplayObject">
                        <StackPanel Orientation="Vertical" VerticalAlignment="Center" Spacing="16" Grid.Column="0" Margin="16">
                            <Image Source="{x:Bind Icon}" Width="125" Stretch="Uniform" HorizontalAlignment="Center" />
                            <!--<TextBlock Text="{x:Bind Name}" FontSize="20" IsTextSelectionEnabled="True" HorizontalAlignment="Center" />-->
                            <TextBlock Text="{x:Bind Subtitle}" FontSize="14" IsTextSelectionEnabled="True" HorizontalAlignment="Center" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </StackPanel>
    </Grid>
</Page>
